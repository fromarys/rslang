export enum ERegisterButtonText {
  register = 'Register',
  login = 'Login',
  cancel = 'Cancel',
}

export enum ERoutes {
  main = '/',
  textbook = 'textbook',
}

export enum EUserWordStatus {
  studied = 'studied',
  normal = 'normal',
  difficult = 'difficult',
}
