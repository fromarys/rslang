export enum ERegisterButtonText {
  register = 'Register',
  login = 'Login',
  cancel = 'Cancel',
}

export enum ERoutes {
  main = '/',
  textbook = 'textbook',
  audiocall = 'audiocall',
  sprint = 'sprint',
  statistics = 'statistics',
}

export enum EUserWordStatus {
  studied = 'studied',
  normal = 'normal',
  difficult = 'difficult',
}
